export const FREE_SHIPPING_THRESHOLD =
  Number(process.env.FREE_SHIPPING_THRESHOLD) || 120;
export const SHIPPING_COST = Number(process.env.SHIPPING_COST) || 15;
export const TAX = Number(process.env.TAX) || 0.13;
